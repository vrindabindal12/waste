const config = {
  plugins: {
    "@tailwindcss/postcss": {}, // Use the explicit PostCSS plugin
    autoprefixer: {},          // Optional but recommended for vendor prefixes
  },
};

export default config;